# Bug log

Append one line per fixed bug. Format:

```
YYYY-MM-DD | file.js:line | symptom → root cause → fix
```

Keep it terse — this is pattern memory across sessions, not a write-up. Scan it before starting a new bug to spot repeat patterns in the same area of code.

---
